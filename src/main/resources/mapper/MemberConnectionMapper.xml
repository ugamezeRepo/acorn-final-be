<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.finals.mapper.MemberConnectionMapper">
    <select id="findAllByChannelId" resultType="memberConnectionEntity">
        (select * from member_connection)
        join
        (
        (select nickname, hashtag from member)
        join
        (select member_id as id from channel_member where channel_id = ${channelId})
        using id
        )
        using nickname, hashtag
    </select>

    <insert id="insert">
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            select member_connection_seq.nextval from dual
        </selectKey>
        insert into member_connection (id, nickname, hashtag, status)
        values (#{id}, #{nickname}, #{hashtag}, #{status})
    </insert>

    <update id="update">
        update member_connection
        set nickname = #{nickname}, hashtag = #{hashtag}, status = #{status}
        where id = #{id}
    </update>

    <delete id="deleteByNicknameAndHashtag">
        delete from member_connection
        where nikname = #{nickname} and hashtag = #{hashtag}
    </delete>
</mapper>